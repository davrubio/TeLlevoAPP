export interface IUser {
    name: string;
    last_name: string;
    institutional_email: string;
    career: string;
    username: string; /* podria ser solo con correo institucional? */
    password: string;
}